//**************Correction avec événement html dans l'input********
//création d'une variable qui correspond à la chaîne de caractères du message
//de la modale qui va s'afficher avec la méthode alert, et renvoyer sa value,
// saisie ciblée avec la avec la méthode getElementById et fonction getInput
function getInput(){
  var inputAlert = document.getElementById('input').value;
  alert(inputAlert);
}
